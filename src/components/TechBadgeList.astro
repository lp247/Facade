---
import TechBadge from "@components/TechBadge.astro";

interface Props {
    techs: string[];
}

const {techs} = Astro.props as Props;
---

<div class:list={["flex row border-box gap-1.5 place-content-start flex-wrap"]}>
    {techs.map((tech) => <TechBadge tech={tech} />)}
</div>
